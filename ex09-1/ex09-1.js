const add = document.getElementById("add");
const remove = document.getElementById("remove");

add.addEventListener("click",)

remove.addEventListener("click", (event) => {
    const last = ul.lastElementChild;
    ul.removeElement(last);
})